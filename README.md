What needed to be in the slides in order to generate the most quality output:

- Remove (delete) unused slides if it is not used inside the document writing later on.
- Remove any border on all images before submitting to the system.
-
